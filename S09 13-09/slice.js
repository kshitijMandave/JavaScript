/*

Slice
------
it it used to extract some elemnt from array.
it is not changing a array it always gives a new array.

*/

var arr = [10, 20, 30, 40, 50, 60];

var sliceval = arr.slice(2, 4); // always add starting index + last index +1 with comma

console.log(sliceval);