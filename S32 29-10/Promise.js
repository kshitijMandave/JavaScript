let promiseObj = new Promise(function (resolve, reject) {});

console.log(promiseObj);

// if output is : Promise { <pending> }

/* then it indicate that promise is created but, there is no data

then how to add data inside promise object?
-------------------------------------------
using two predefined function resolve and reject*/
