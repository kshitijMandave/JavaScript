/*
Map
------
it is a predefined function of Arrary
it s also used to access each element of Array. it returns new array. and gives values in boolean. */

var arr = [10, 20, 30, 40, 50, 6, 0, 4, 70, 8, 5];

let Mapped = arr.map((element, index) => {
  return element >= 10;
});

console.log(map);
